<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Welcome,</title>
  </head>
  <body>

<% if logged_in? %>
  <%= erb :'users/logout' %>
<% else %>
  <p><a href="/login">Login</a> | <a href="/signup">Sign up</a></p>
<% end %>
    <ul>
    <% @tweets.each do |t| %>
      <li> <%= "#{t.user.username}: '#{t.content}'" %> </li>
    <% end %>
    </ul>



  </body>
</html>
